name: Sincronizar CAJA

# Dispara la sincronización planilla -> base de datos llamando al endpoint de Netlify.
# El disparo AUTOMÁTICO lo maneja cron-job.org (más confiable y puntual que el cron
# de GitHub). Acá queda solo el disparo MANUAL (Run workflow) como respaldo.
on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Modo de sincronización"
        type: choice
        options: [incremental, full]
        default: full

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Determinar modo
        id: mode
        run: |
          if [ "${{ github.event.schedule }}" = "0 3 * * *" ]; then
            echo "mode=full" >> "$GITHUB_OUTPUT"
          elif [ -n "${{ github.event.inputs.mode }}" ]; then
            echo "mode=${{ github.event.inputs.mode }}" >> "$GITHUB_OUTPUT"
          else
            echo "mode=incremental" >> "$GITHUB_OUTPUT"
          fi

      - name: Disparar sincronización
        env:
          SECRET: ${{ secrets.SYNC_SECRET }}
        run: |
          echo "Modo: ${{ steps.mode.outputs.mode }}"
          # Esperado tras el fix del middleware: status=202 y location vacío (llega a la función).
          curl -sS -o /dev/null -w "status=%{http_code}  location=%{redirect_url}\n" \
            "https://mictacte.netlify.app/.netlify/functions/sync-background?mode=${{ steps.mode.outputs.mode }}&secret=$SECRET" \
            -H "x-sync-secret: $SECRET"
