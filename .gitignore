.env.local
.env*.local
node_modules/
.next/
out/
*.tsbuildinfo

# Salidas del análisis del Excel (contienen datos reales de clientes — no commitear)
scripts/analisis-excel.json
scripts/pivots.json

# Backups de datos generados localmente (contienen datos reales del negocio)
backups/
